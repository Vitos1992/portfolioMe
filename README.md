# portfolioMe

<!-- ==== ПЕРЕПРОШУЮ ЗА російску === -->

1) Перший запит був сформований таким чином:

Сгенерируй hero-секцию для личного сайта портфолио разработчика

Условия:
1. Используй чистый HTML + CSS (без фреймворков).
2. Внутри блока должно быть 
   -имя пользователя (в виде  <h1>),
   -краткое bio-описание (1-2 предложения <p>),
   -подзаголовок «Мой путь в IT + AI»,
3. Добавь одну кнопку CTA «Мои навыки».
4. Стили, минимализм, отцентрированный контент, адаптивность.
   -фон - темный (#0b0b0b) с легким градиентом,
   -текст - светлый,
   -акцент - цвет - Через перемену --accent,
5. Добавь легкую анимацию появления текста при загрузке (через opacity + translateY).
6. Сделай код читабельным и понятным для новичка.
7. Структура проекта строго:
    - index.html
    - style.css
    - script.js  

-- а так же  добавь динамики. Давай добавим легкую    анимацию с взаимодействием с пользователем (например, с курсором миши) и автотомние легкие анимации но при єтом текст не подвижний а на курсор миши реагируєт анимация на заднем фоне

2) 

Теперь сгенерируй интерактивный блок "Про мене", в новой секциї (class="about")
Требованияь
1.HTМl-структура

заголовок <h2> "Про мене",
контейнер для описе в <p>,
фото в <img>
Стили:
лёгкая тень hover-эффект,
акцент-цвет через var(--accent),
адаптивность.
Код чистый, читаемый, без внешних библиотек|

-- перепроверь мой код и найди ошибку потомуч то я не вижу блок секции "Про мене"

3) 


Теперь сгенерируй интерактивный блок "Мои навыки", в новой секциї (class="skills")
Требованияь
1.нTМl-структура
- заголовок <h2> "Мої навички",
- контейнер для карточек навыков (<di ld_"skills-grid">), 
- кнопка "Дoдати навички"
2. Каждая карточка навыка содержит:
- название (в <h3>),
- короткое описание (в <р>), 
- при клике карточка разворачиваетсяјсворачивается.
3. Напиши JS-логикуз
- добавление нового навыка через prompt(),
- сохранение всех карточек b localstorage,
- восстановление при перезагрузке страницы.
4. Стили:
- сетка карточек (grid), 
- лёгкая тень hover-эффект, 
- акцент-цвет через var(--accent), 
- адаптивность.
5. Код чистый, читаемый, без внешних библиотек|


4)  

Теперь сгенерируй мне <header> 
Требования
1.HTМl-структура

логотип в <img> 
контейнер c списком в <li> блок списка с "Про мене", "Мої навички", "Проекти"
добавь кнопку с переводом страници с українского на английский и наоборот
Стили:
лёгкая тень hover-эффект,
акцент-цвет спетлей и легкая прозрачнасть основного body 
адаптивность и зафиксировать header
Код чистый, читаемый, без внешних библиотек|

-- перепроверь мой код и найди ошибку потомуч то я не вижу блок секции "Про мене"

-- теперь давай сгенерируює в бллоке ""Проекти" карточки проектов с аналогичной логикой как и в блоке "Мои навыки" но с добавлением ссылки на проект и изображением проекта в карточке. И так же сохранять все карточки проектов в localstorage и восстанавливать при перезагрузке страницы. Стили аналогично блоку "Мои навыки". Код чистый, читаемый, без внешних библиотек|

5) 

Теперь сгенерирую мне блок <footer>

1. HTML - структура
-- контейнер со списком в <li> блок списка с иконками социальних  сетей с силками перехода по ним: нистраграмм, телеграм, линкедин, вотцап

2. Стили продублируем с <header>
--адаптивность
- сетка карточек (grid), 
- лёгкая тень hover-эффект, 
- акцент-цвет через var(--accent), 
- адаптивность.
3. Код чистый, читаемый, без внешних библиотек|